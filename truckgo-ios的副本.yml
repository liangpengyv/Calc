flows:
  - name: debug
    language: objc
    version: Xcode8

    trigger:
      push:
        - master
      pull_request: true

    steps:
      - name: init_debug
        plugin:
          name: objc_init

      - name: Git Clone debug
        plugin:
          name: git

      - name: Pod Install
        plugin:
          name: objc_install

      - name: fastlane build and archive
        scripts:
          - fastlane gym --scheme Calc --silent --clean

      # - name: fir upload
      #   enable: true
      #   failure: false
      #   plugin:
      #     name: fir_plugin
      #     inputs:
      #       - FIR_API_TOKEN=d040268a33bd1874c299812a4f96aabe
      #       - FIR_CHANGELOG='test change log $FLOW_EVENT_NUMBER'

    finalize:
      email:
        success:
          - mrliangpengyv@163.com
        failure:
          - mrliangpengyv@163.com
